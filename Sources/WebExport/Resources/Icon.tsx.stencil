// {% include "header.stencil" %}

import type { IconProps } from './types';

export const {{ componentName }} = ({ color = 'currentColor', size = 24, ...props }: IconProps) => (
  <svg
    xmlns="http://www.w3.org/2000/svg"
    width={size}
    height={size}
    viewBox="{{ viewBox }}"
    fill="none"
    {...props}
  >
    {{ svgContent }}
  </svg>
);
